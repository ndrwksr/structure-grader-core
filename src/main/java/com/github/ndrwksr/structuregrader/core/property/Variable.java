package com.github.ndrwksr.structuregrader.core.property;

/**
 * A type alias for the interfaces that all variables (fields, parameters, methods) implement.
 */
public interface Variable extends Typed, Annotated, Modified {
}
